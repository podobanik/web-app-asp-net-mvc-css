@using WebAppAspNetMvcHtml.Models;
@model ClientType

@using (Html.BeginForm("Edit", "ClientTypes", FormMethod.Post))
{
    @Html.ValidationSummary();
    @Html.EditorForModel()
    <input type="submit" value="Сохранить" />
    @Html.ActionLink("Отменить", "Index", "ClientTypes")
}
